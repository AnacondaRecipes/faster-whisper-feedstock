# the conda-build parameters to use
build_parameters:
  - "--suppress-variables"
  - "--error-overlinking"

build_env_vars:
  ANACONDA_ROCKET_ENABLE_PY314: yes

# ctranslate2 is building in staging
channels:
  - https://staging.continuum.io/pbp/ctranslate2-4.7.1-staging
